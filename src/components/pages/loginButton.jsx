// import * as React from 'react';
// import Button from '@mui/material/Button';
// import { LOGIN } from '../model/mutations/login';
// import { useMutation } from "@apollo/client";



// export default function SuspendDoctorButton(props) {
 
//  const handleLogin = (event)=>{
//   event.preventDefault();
   
//    login({ variables: { id: props.id} });
//     handleClose();

//  }

// const[login,{loading,error,data}] = useMutation(SUSPEND_DOCTOR

// );
// if (loading) return <p>loading</p>;
// if (error) return <p>Error : {error.message}</p>;
  
//   return (
   
//     <div className=''>


//       <Button  onClick={handleLogin} variant="text" size="small">
//        Login
        
//       </Button>
     
//     </div>
//   );
// }